<%= form_with(model: challenge, local: true, :multipart => true) do |form| %>
  <% if challenge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(challenge.errors.count, "error") %> prohibited this challenge from being saved:</h2>

      <ul>
      <% challenge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :challenge_title %>
  </div>

  <div class="field">
    <%= form.label :challenge %>
    <%= form.file_field :challenge, id: :challenge_challenge %>
  </div>

  <div class="field">
    <%= form.label :test %>
    <%= form.file_field :test, id: :challenge_test %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
