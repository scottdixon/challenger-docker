<%= form_with(model: submission, local: true) do |form| %>
  <% if submission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(submission.errors.count, "error") %> prohibited this submission from being saved:</h2>

      <ul>
      <% submission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :challenge_id %>
    <%= form.collection_select :challenge_id, Challenge.all, :id, :title %>
  </div>

  <div class="field">
    <%= form.label :submission %>
    <%= form.file_field :submission, id: :submission_submission %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
