container_commands:
  install_docker:
    command: "sudo yum install -y docker"
  add_webapp_to_docker:
    command: "sudo usermod -a -G docker webapp"
  start_docker:
    command: "sudo service docker start"
